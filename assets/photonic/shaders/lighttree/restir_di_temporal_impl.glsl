#ifndef PHOTONICS_RESTIR_DI_TEMPORAL_IMPL_GLSL
#define PHOTONICS_RESTIR_DI_TEMPORAL_IMPL_GLSL

// Thin DI temporal bridge surface. The full temporal ownership/scatter
// implementation currently remains in reuse_bridge.glsl so the DI temporal
// passes can share the existing validated definitions without duplicating
// symbols or drifting from the active pipeline behavior.

#endif
